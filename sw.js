// A minimal service worker to enable the PWA installation prompt.
self.addEventListener('fetch', (event) => {
  // This service worker is intentionally left blank.
  // It's present to make the web app installable.
});
